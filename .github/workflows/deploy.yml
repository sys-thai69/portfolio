name: Deploy

on:
  push:
    branches: [main]

jobs:
  # First run CI checks
  ci:
    name: CI Checks
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Check TypeScript types
        run: npm run type-check

      - name: Run ESLint
        run: npm run lint

      - name: Check Prettier formatting
        run: npm run format:check

      - name: Build project
        run: npm run build

  # Deploy to Vercel (uncomment and configure when ready)
  deploy:
    name: Deploy to Vercel
    needs: ci
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Deploy to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.yP1yIOmcIV2oc1fzsXhQ9GSg }}
          vercel-org-id: ${{ secrets.skyvas-projects }}
          vercel-project-id: ${{ secrets.83GXZbZh5zw6ZaMYfrrBnqBa1PB9 }}
          vercel-args: "--prod"
